<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >  
<mapper namespace = "userMapper">
	<resultMap type="com.car.model.bean.User" id="userMap">
		<result column="id" property="id"/>
		<result column="username" property="username"/>
		<result column="password" property="password"/>
		<result column="realname" property="realname"/>
	</resultMap>

	<select id="findUserByProperty" parameterType="hashmap" resultType="com.cat.model.bean.User">
		select * from sys_user
		<where>
			<if test="id != null and id != ''">
				id = #{id}
			</if>
			<if test="username != null and username != ''">
				username = #{username}
			</if>
			<if test="realname != null and realname != ''">
				realname = #{realname}
			</if>
		</where>
	</select>

	<insert id="addUser" parameterType="com.cat.model.bean.User">
		insert into sys_user(username,password,realname)
		values(#{username},#{password},#{realname})
	</insert>


	<update id="updateUser" parameterType="com.cat.model.bean.User">
		update sys_user
		<set>
			<if test="password != null and password != '' ">
				password = #{password},
			</if>
			<if test="realname != null and realname != '' ">
				realname = #{realname},
			</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="deleteUser" parameterType="com.cat.model.bean.User">
		delete from sys_user
		<where>
			<if test="username != null and username != ''">
				username = #{username}
			</if>
		</where> 
	</delete>
</mapper>